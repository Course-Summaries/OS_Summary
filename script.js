document.addEventListener("DOMContentLoaded", () => {
    console.log("Welcome to the OS Summary page!");

    // Highlight active section if needed in the future
    const sections = document.querySelectorAll("section");
    const nav = document.querySelector("nav");

    // You can expand interactivity here (e.g., scroll tracking or popups)
});
